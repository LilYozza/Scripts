print("test 1")
local w = library:CreateWindow("Main")
local Auto = w:CreateFolder("Auto")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer

local character
local root
local TURN_SPEED = 1
local holdingW = false

-- prevents flicker when tween ends / micro jitter happens
local moveTimer = 0
local moveTime = 0
local REQUIRED_MOVE_TIME = 0.3
local STOP_DELAY = 0
local TELEPORT_DISTANCE = 25 -- adjust for your game

Auto:Slider("move time",{
    min = 0; max = 5; precise = false;
},function(value)
	REQUIRED_MOVE_TIME = value
    print("REQUIRED_MOVE_TIME", value)
end)

Auto:Slider("stop delay",{
    min = 0; max = 5; precise = false;
},function(value)
	STOP_DELAY = value
    print("STOP_DELAY", value)
end)

local lastPosition

local function setupCharacter(char)
	character = char
	root = character:WaitForChild("HumanoidRootPart")

	lastPosition = root.Position
	moveTimer = 0

	-- safety: release W on death immediately
	local hum = character:WaitForChild("Humanoid")
	hum.Died:Connect(function()
		if holdingW then
			holdingW = false
			VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.W, false, game)
		end
	end)
end
setupCharacter(player.Character or player.CharacterAdded:Wait())
player.CharacterAdded:Connect(setupCharacter)

local con
local TELEPORT_DISTANCE = 25 -- adjust for your game

Auto:Toggle("Helper", function(bool)
    shared.ToggleTH = bool

    spawn(function()
        if shared.ToggleTH then
            con = RunService.RenderStepped:Connect(function(dt)
                if not root or not root.Parent then return end

                local currentPosition = root.Position
                local movement = currentPosition - lastPosition

                -- Ignore teleports
                if movement.Magnitude > TELEPORT_DISTANCE then
                    lastPosition = currentPosition
                    moveTime = 0
                    moveTimer = STOP_DELAY
                    return
                end

                if movement.Magnitude > 0.01 then
                    moveTime = moveTime + dt
                    moveTimer = STOP_DELAY

                    local direction = movement.Unit
                    local flatDir = Vector3.new(direction.X, 0, direction.Z)

                    if flatDir.Magnitude > 0 then
                        flatDir = flatDir.Unit

                        local yaw = math.atan2(-flatDir.X, -flatDir.Z)
                        local _, currentYaw, _ = root.CFrame:ToOrientation()

                        root.CFrame = CFrame.new(root.Position) *
                            CFrame.Angles(
                                0,
                                currentYaw + (yaw - currentYaw) * TURN_SPEED,
                                0
                            )
                    end

                    -- Only trigger W if movement is stable for a bit
                    if moveTime >= REQUIRED_MOVE_TIME and not holdingW then
                        holdingW = true
                        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.W, false, game)
                    end
                else
                    moveTime = 0
                    moveTimer = moveTimer - dt

                    if moveTimer <= 0 and holdingW then
                        holdingW = false
                        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.W, false, game)
                    end
                end

                lastPosition = currentPosition
            end)
        else
            if con then
                con:Disconnect()
                con = nil
            end

            if holdingW then
                holdingW = false
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.W, false, game)
            end
        end
    end)
end)

Auto:Button("Script",function()
    spawn(function()
		loadstring(game:HttpGet("https://www.luxyhub.space/api/loader/luxyhub"))()
    end)
end)