local library = loadstring(game:HttpGet(('https://raw.githubusercontent.com/LilYozza/Scripts/main/Misc/UILibrary.txt')))()w = library:CreateWindow("Main")

local Players=game:GetService("Players")
local Player=game.Players.LocalPlayer

local Auto = w:CreateFolder("Auto")

--Base Variables
local Tycoon, NilTycoon, SCollect
local Tycoons=Workspace:WaitForChild("Tycoons")
local Button=Player.PlayerGui.Spinner.SpinnerNormal.Reward.Claim

-- Non Return Functions
local function Touch(p,t) firetouchinterest(Player.Character:WaitForChild("HumanoidRootPart"),p,0)firetouchinterest(Player.Character:WaitForChild("HumanoidRootPart"),p,1)task.wait(t)end

--Getting Data
local function GetTycoon() 
    for _,v in pairs(Tycoons:GetChildren())do 
        if v:FindFirstChild("Values"):FindFirstChild("Owner") then
        if v.Values.Owner.Value==Player then Tycoon=v break end 
        if v.Values.Owner.Value==nil and NilTycoon==nil then NilTycoon=v end end end
    if Tycoon==nil then Tycoon=NilTycoon Touch(Tycoon.Important.Door,0) Player.Character:WaitForChild("HumanoidRootPart").CFrame=Tycoon.Important.Door.CFrame end end
repeat GetTycoon()task.wait()until Tycoon~=nil

--Post Variables
local Buttons=Tycoon:FindFirstChild("Buttons")
local Important=Tycoon:FindFirstChild("Important")
local CashCollect=Important:FindFirstChild("CashCollect")
local Values=Tycoon:FindFirstChild("Values")
local Boxes=Workspace:FindFirstChild("Boxes")

Auto:Toggle("Auto Collect",function(bool)
    shared.toggle1 = bool
    spawn (function()while shared.toggle1 do Touch(CashCollect.CollectArea,1) task.wait() end end)
end)

Auto:Toggle("Smart Collect",function(bool)
    SCollect=bool
end)

Auto:Toggle("Auto Buy",function(bool)
    shared.toggle1 = bool
    spawn (function()
        while shared.toggle1 do
            for _,v in pairs(Buttons:GetChildren())do
                for key,value in pairs(v:GetChildren())do
                    if value.Transparency==0 and not value.Info.Price.Main.Text:match("R") then
                        if Player.PlayerStats.Money.Value+Values.Money.Value >= value.Price.Value and SCollect then Touch(CashCollect.CollectArea,0.3) end
                        if Player.PlayerStats.Money.Value >= value.Price.Value then Touch(value,0.3) end
                    end
                end
            end
        	task.wait()
        end
    end)
end)

Auto:Toggle("Auto Creates",function(bool)
    shared.toggle1 = bool
    spawn (function()
        while shared.toggle1 do
            for _,v in pairs(Boxes:GetDescendants()) do if v:IsA("TouchTransmitter") then if v.Parent.Parent:FindFirstChild("isGroupBox").Value==true then Touch(v.Parent,1) task.wait() firesignal(Button['MouseButton1Click']) end end end
        	task.wait()
        end
    end)
end)

Auto:Button("Get Items",function()
    spawn(function()for _,v in pairs(Tycoons:GetDescendants()) do if v:IsA("TouchTransmitter") and v.Parent.Name=="Get" then Touch(v.Parent,0.3) end end end)
end)

Auto:Button("Remove Doors",function()
    spawn(function() for _,v in pairs(Tycoons:GetChildren()) do if v:FindFirstChild("Builds") then for key,value in pairs(v.Builds:GetDescendants()) do if value.Name:match("Laser") then value:Destroy() end end end end end)
end)

local Miscload = loadstring(game:HttpGet('https://raw.githubusercontent.com/LilYozza/Scripts/main/Misc/Misc.txt'))()